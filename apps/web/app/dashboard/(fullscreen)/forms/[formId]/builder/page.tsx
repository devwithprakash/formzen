import { FormBuilder } from "@/components/form-builder/form-builder"

export const metadata = {
  title: "Create Form — FormZen",
}

export default function NewFormPage() {
  return <FormBuilder />
}
